<Virtualhost *:80>
    ServerAdmin test@test.com
    ServerName docker.local
    ServerAlias www.docker.local
    # DocumentRoot /var/www/docker.local/public

    # <Directory /var/wwww/docker.local/public>
    #     Options -Indexes +FollowSymlinks
    #     AllowOverride All
    #     Require all granted
    # </Directory>
    
    Redirect permanent / https://docker.local
</Virtualhost>