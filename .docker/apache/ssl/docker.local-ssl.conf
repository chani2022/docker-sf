<IfModule mod_ssl.c>
    <VirtualHost *:443>
            ServerAdmin test@test.com
            ServerName docker.local
            DocumentRoot /var/www/docker.local/public

            ErrorLog ${APACHE_LOG_DIR}/error.log
            CustomLog ${APACHE_LOG_DIR}/access.log combined

            SSLEngine on
            SSLCertificateFile      /var/www/docker.local/.docker/apache/ssl/docker.local.pem
            SSLCertificateKeyFile  /var/www/docker.local/.docker/apache/ssl/docker.local-key.pem

            SSLProtocol -ALL +TLSv1 +TLSv1.1 +TLSv1.2
            SSLHonorCipherOrder On
            SSLCipherSuite ECDHE-RSA-AES128-SHA256:AES128-GCM-SHA256:HIGH:!MD5:!aNULL:!EDH:!RC4
            SSLCompression off

            <Directory /var/www/docker.local/public>
                Options -Indexes +FollowSymLinks +MultiViews
                AllowOverride All
               Require all granted
	        </Directory>
            # <FilesMatch "\.(cgi|shtml|phtml|php)$">
            #     SSLOptions +StdEnvVars
            # </FilesMatch>
            # <Directory /usr/lib/cgi-bin>
            #     SSLOptions +StdEnvVars
            # </Directory>
    </VirtualHost>
</IfModule>